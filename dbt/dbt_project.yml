name: "urban_transit"
version: "1.0.0"
config-version: 2

profile: "urban_transit"

model-paths: ["models"]
snapshot-paths: ["snapshots"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  urban_transit:
    staging:
      +materialized: view
      +schema: staging
    gold:
      +materialized: table
      +schema: gold
